# Common
server.port=${port:8080}

# DB
spring.jpa.database=mysql
spring.datasource.url=jdbc:mysql://localhost:3306/java_ecommerce_api?useTimezone=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=skye

# [create] drop e cria o db toda vez que a aplicação é rodada | [nome] nenhuma ação
spring.jpa.hibernate.ddl-auto=create 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true